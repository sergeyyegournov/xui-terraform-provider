---
page_title: "xui_panel_settings Resource - xui"
subcategory: ""
description: |-
  Manages 3x-ui panel settings (/panel/setting/update). This is a singleton resource — only one instance should exist per panel.
---

# xui_panel_settings (Resource)

Manages 3x-ui panel settings (`/panel/setting/update`). This is a singleton resource — only one instance should exist per panel. All attributes are optional and default to the panel's built-in defaults. Set `restart_panel` to true if you want to restart the panel after applying changes (required for web listen/port/cert changes to take effect).

## Example Usage

```terraform
resource "xui_panel_settings" "this" {
  web_port      = 2053
  web_base_path = "/my-secret-path/"
  web_cert_file = "/etc/letsencrypt/live/example.com/fullchain.pem"
  web_key_file  = "/etc/letsencrypt/live/example.com/privkey.pem"

  session_max_age = 120
  time_location   = "Europe/Tallinn"

  tg_bot_enable       = true
  tg_bot_token        = var.tg_bot_token
  tg_bot_chat_id      = var.tg_chat_id
  tg_bot_login_notify = true

  sub_enable = true
  sub_port   = 2096
  sub_path   = "/my-sub/"

  restart_panel = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) Static resource id (`panel-settings`).

### Optional — Web Server

- `web_listen` (String) Web panel listen IP address (empty = all interfaces). Default `""`.
- `web_domain` (String) Web panel domain for validation. Default `""`.
- `web_port` (Number) Web panel port. Default `2053`.
- `web_cert_file` (String) Path to SSL certificate file for the web panel. Default `""`.
- `web_key_file` (String) Path to SSL private key file for the web panel. Default `""`.
- `web_base_path` (String) Base path for panel URLs (e.g. `/<uuid>/`). Default `"/"`.
- `session_max_age` (Number) Session maximum age in minutes. Default `60`.

### Optional — UI

- `page_size` (Number) Number of items per page in lists. Default `50`.
- `expire_diff` (Number) Expiration warning threshold in days. Default `0`.
- `traffic_diff` (Number) Traffic warning threshold percentage. Default `0`.
- `remark_model` (String) Remark model pattern for inbounds. Default `""`.
- `datepicker` (String) Date picker format. Default `"gregorian"`.

### Optional — Telegram Bot

- `tg_bot_enable` (Boolean) Enable Telegram bot notifications. Default `false`.
- `tg_bot_token` (String, Sensitive) Telegram bot token. Default `""`.
- `tg_bot_proxy` (String) Proxy URL for Telegram bot. Default `""`.
- `tg_bot_api_server` (String) Custom API server for Telegram bot. Default `""`.
- `tg_bot_chat_id` (String) Telegram chat ID for notifications. Default `""`.
- `tg_run_time` (String) Cron schedule for Telegram notifications. Default `"@daily"`.
- `tg_bot_backup` (Boolean) Enable database backup via Telegram. Default `false`.
- `tg_bot_login_notify` (Boolean) Send login notifications via Telegram. Default `false`.
- `tg_cpu` (Number) CPU usage threshold percentage for Telegram alerts. Default `80`.
- `tg_lang` (String) Telegram bot language. Default `"en-US"`.

### Optional — Security

- `time_location` (String) Time zone location (e.g. `UTC`, `Asia/Tehran`). Default `"UTC"`.

### Optional — Subscription Server

- `sub_enable` (Boolean) Enable subscription server. Default `false`.
- `sub_json_enable` (Boolean) Enable JSON subscription endpoint. Default `false`.
- `sub_title` (String) Subscription title. Default `""`.
- `sub_support_url` (String) Subscription support URL. Default `""`.
- `sub_profile_url` (String) Subscription profile URL. Default `""`.
- `sub_announce` (String) Subscription announcement. Default `""`.
- `sub_listen` (String) Subscription server listen IP. Default `""`.
- `sub_port` (Number) Subscription server port. Default `2096`.
- `sub_path` (String) Base path for subscription URLs. Default `"/sub/"`.
- `sub_domain` (String) Domain for subscription server validation. Default `""`.
- `sub_cert_file` (String) SSL certificate file for subscription server. Default `""`.
- `sub_key_file` (String) SSL private key file for subscription server. Default `""`.
- `sub_updates` (Number) Subscription update interval in minutes. Default `12`.
- `sub_encrypt` (Boolean) Encrypt subscription responses. Default `false`.
- `sub_show_info` (Boolean) Show client information in subscriptions. Default `false`.
- `sub_uri` (String) Subscription server URI. Default `""`.
- `sub_json_path` (String) Path for JSON subscription endpoint. Default `"/json/"`.
- `sub_json_uri` (String) JSON subscription server URI. Default `""`.
- `sub_json_fragment` (String) JSON subscription fragment configuration. Default `""`.
- `sub_json_noises` (String) JSON subscription noise configuration. Default `""`.
- `sub_json_mux` (String) JSON subscription mux configuration. Default `""`.
- `sub_json_rules` (String) JSON subscription routing rules. Default `""`.
- `sub_enable_routing` (Boolean) Enable routing for subscription. Default `false`.
- `sub_routing_rules` (String) Subscription global routing rules. Default `""`.
- `external_traffic_inform_enable` (Boolean) Enable external traffic reporting. Default `false`.
- `external_traffic_inform_uri` (String) URI for external traffic reporting. Default `""`.

### Optional — Lifecycle

- `restart_panel` (Boolean) If true, restart the panel after applying changes. Required for web listen/port/cert changes to take effect. Default `false`.
